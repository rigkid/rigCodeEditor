# codeEditor hero

Exercises **rigCodeEditor**: TextEditorPanel chrome (menus, find/replace, sidebar),
JetBrains Mono, and a seeded Python sample.

```bash
cmake -S . -B build
cmake --build build
# refresh pack-card shot:
powershell -File ./capture-preview.ps1
```

See [pack README](../../README.md).
